#include "main.h"
/**
 * _abs - compute the absolute value of an int
 *@c: the value
 * Description: The standard library provides a similar funct..
 *
 * Return: (0)
 *
 */
int _abs(int c)
{
	if (c >= 0)

		return (c);
	else
		return (c * -1);
}
